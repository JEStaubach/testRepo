name: github-projects.create-issue
on:
  issues:
    types: [opened]
jobs:
  new_issue:
    name: "github-projects.create-issue"
    runs-on: ubuntu-latest
    steps:
      - name: "greeting"
        uses: JEStaubach/actions/github-projects/create-issue/greeting@dev
        with:
          action-token: "${{ secrets.Access_token }}"
          comment-text: "Thanks for opening this issue!"
          board-name: "triage"
          column-name: "awaiting review"
      - name: "create-card-in-board-column"
        uses: JEStaubach/actions/github-projects/create-issue/create-card-in-board-column@dev
        with:
          action-token: "${{ secrets.Access_token }}"
          board-name: "triage"
          column-name: "awaiting review"
